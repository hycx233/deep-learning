# 本项目的 Agent 协作规则

本文件适用于整个仓库。为三人小组工作的 AI agent 应遵循同一套约定；任务负责人的明确指示优先于这里的默认习惯。

## 项目定位

这是两周内完成的普通课程项目，三人使用 AI agent 辅助开发。目标是把课程要求做完整，让代码能运行、实验能解释、结果能复现。范围和时间安排见 [项目完成计划](docs/项目完成计划.md)。

- 优先实现最小可用流程，再根据实际结果做必要改进。完成 issue 的验收内容即可，不主动扩展产品功能。
- 默认采用计划中的轻量模型和少量实验。不自行加入大型预训练模型、大规模调参或新的选做任务。
- 人工时间比机器等待时间更宝贵。合理的数小时训练可以后台运行，避免让成员反复手动操作或陪跑。
- 普通命名、局部重构、图表排版和小样本验证自行判断并完成，不为低风险、可逆的细节反复请求确认。改变整体任务范围、共享数据口径或占用他人正在使用的资源时再协调。

## 开始任务前

1. 阅读 [README](README.md)、当前 issue 及其依赖。涉及数据时再读 [数据说明](data/README.md)，只查阅与当前任务有关的计划内容和代码。
2. 查看工作区状态，保留他人已有修改和未跟踪文件。不要为了自己的任务清空目录、覆盖他人结果或重置整个分支。
3. 先查现有实现、参数和输入输出格式，能复用就复用。缺少依赖模块时可先用小样本开发，但在交付前接入真实数据。
4. [任务清单](docs/GitHub任务清单.md)中的 P01—P12 是计划编号，实际执行以清单链接的 GitHub issue 为准。组长为 `hycx233`，另两位成员为 `onlysonder`、`lanshuheng9-oss`；发布与分配任务由组长统筹。

## 实现风格：简单够用

- 使用 Python、PyTorch 和计划中的常用科学计算库。优先普通函数、少量模块和清晰的命令行脚本；只有模型或确有状态的对象需要类。
- 不为本项目引入 Web 服务、数据库、插件机制、通用训练平台、多层抽象、依赖注入、复杂配置继承或微服务。没有真实需要时，不做多框架、多设备、多格式的兼容层。
- 输入文件存在、矩阵形状、坐标范围等实际边界要检查，出错时给出可定位的信息。不要堆砌假设性防御代码，不要用宽泛的 `except` 吞掉错误、静默切换算法或生成假数据来继续执行。
- 参数优先使用 `argparse` 和合理默认值；多个模块确实共享的设置集中保存，避免各写一份。不要新增配置框架。
- 添加依赖前先看已有依赖。需要的新库写入统一的依赖文件并说明用途；记录实际验证过的版本，不直接导出个人环境的全部包。
- 代码标识符用清楚的英文和 `snake_case`，类名用 `PascalCase`；文档、注释和 PR 说明默认中文。注释解释原因与数据含义，不逐行复述代码。
- 不硬编码某位成员的绝对路径、用户名或 GPU 编号。输入路径、输出目录和运行设备应可指定；示例命令从仓库根目录执行。

## 文件与共享接口

按实际需要创建目录，不提前生成大量空目录和占位模块。开始写代码后优先使用以下布局，已有合理实现则继续沿用，不仅为目录偏好迁移文件。

| 位置 | 用途 |
| --- | --- |
| `scripts/` | 数据处理、训练、评估、发现、差异分析与绘图的命令入口 |
| `src/` | 被多个入口复用的读取、预处理、模型或绘图代码 |
| `data/` | 课程数据、小型标注与数据说明；原始大文件按 `.gitignore` 保留在本地 |
| `outputs/<任务>/<运行名>/` | 本地生成的权重、缓存、日志与批量图表，避免覆盖别人的运行 |
| `docs/` | 计划、方法说明和报告文稿 |
| `docs/results/` | 经人工挑选、准备纳入报告的小型结果表与代表图 |
| `docs/report/` | LaTeX 报告源文件，主文件 `main.tex`，各成员的章节放在 `sections/` |
| `docs/slides/` | 最终选定的 Beamer 或 PPT 演示文稿源文件 |

- 矩阵读取、归一化、窗口截取和坐标转换共用一套实现，不为分类、发现、差异分析各复制一份。
- 数据表字段沿用已有定义。结构记录至少保留 ID、type、chrom、start、end、center、length_bp；窗口记录保留位置、sample_id、condition、replicate。不要随意重命名上下游已使用的列。
- 修改共享接口时，更新实际调用方，并在 PR 中说明变化；若会与另一成员正在开发的模块冲突，先在关联任务中对齐。
- 原始矩阵与 Excel 保持不变，清洗和转换产物另存。提交前检查 `.gitignore`，不使用强制添加把大数据、模型权重或批量输出塞进 Git。

## 数据与实验约定

- 按数据说明统一 `MG1655` / `NC_000913.3` 染色体别名。内部坐标使用 0-based、左闭右开；先核对来源口径再转换，保留需要追溯的原始字段。CHIN 的原有 Center 字段不要擅自重算。
- 使用稀疏、分块或局部读取；不要构造全基因组的 10 bp 稠密矩阵。两个 WT 文件没有现成的 `weight` 列，不能假定 `balance=True` 就能直接读取。
- 强度比较与形态分析沿用共享归一化方案。不要把每个窗口独立缩放后的结果用于比较不同条件的接触强弱。
- 训练、验证、测试使用共享分组，同一结构的重复、增强样本和重叠窗口不跨集合。用验证集调整模型，测试结果留到最终评价。
- 一次正式运行至少保留：运行命令、随机种子、数据划分、关键参数、所用代码版本和结果文件位置。一份小配置或 JSON 加简短记录就够，不引入实验管理平台。
- 先用少量真实窗口确认流程，再启动完整训练。共享 GPU 按约定顺序使用，不中断他人的进程；各模块负责人提供可直接运行的命令并负责解读自己的结果。
- 指标、图表和文字必须来自实际运行。区分已经验证、仅做小样本验证和仍待运行的部分；没有可信的新结构时，如实报告已知结构复现及候选排除结果。

## 验证做到与改动相称

- 文档与小型配置改动：检查内容、链接和必要格式，不跑完整训练。
- 数据或模型脚本改动：至少跑一次少量真实数据的相关入口；训练代码可先跑一个小批次，检查形状、损失和输出。
- 坐标转换、数据划分等容易影响结论的逻辑，可补少量有意义的断言或针对性测试。没有覆盖率指标，不为简单脚本引入庞大测试体系，也不写只复述实现的测试。
- 正式指标沿用保存的正式实验结果；仅在算法、数据或参数变化会影响结果时重跑。无需为每次 PR 都重跑完整训练。
- 图表要实际打开检查至少一个代表输出；批量图另检查首尾区间。核对坐标、标签、色标与图例是否可读。
- 缺少数据或 GPU 时，完成不受影响的工作并说明哪些验证尚未执行，不声称测试或训练已通过。

## 报告与演示文稿

- 最终报告采用 LaTeX，提交编译出的 PDF，同时保留 `.tex`、引用和所需图片。组长建立一个简单的主文件，三人分别编辑自己负责的章节，通过 `\input` 合并，避免同时重写 `main.tex`。
- 默认使用简洁的 `ctexart` 与 XeLaTeX，不自行设计文档类或复杂构建系统。记录一条已验证的编译命令，构建产物放到 `docs/report/build/`，正式提交 PDF 放到 `dist/`。
- 报告中保留专门的“设计思路”章节，图、表、指标来自已保存的实验结果。章节修改后编译检查相关页面；不只检查编译退出码。
- 演示文稿可用 LaTeX Beamer 或 PPT，在结果定稿时统一选一种格式和模板，不要求两套都做。成员分别准备自己的讲解内容，由 `onlysonder` 合并和录屏合成；演示文稿不能代替最终讲解视频。

## GitHub 与交付习惯

- 已下发的开发任务从最新 `main` 建分支，名称如 `feat/12-classifier`、`fix/18-window-coordinates`、`docs/20-report`。通常一个 issue 对应一个 PR，共同报告可拆成各自章节的 PR。
- 日常开发通过 PR 合入 `main`，一名其他成员审核即可。仓库初始化或组长明确要求的直接整理可按其指示提交，不额外设置审批层级。
- 提交保持小而完整，消息格式如 `feat: 增加已知结构分类入口`、`fix: 修正窗口边界`、`docs: 补充实验结果`。只暂存当前任务涉及的文件，不夹带全仓库格式化或无关重构；沿用已有 Git 身份和签名配置。
- PR 使用仓库模板，写清改动、实际运行命令/检查方式、代表结果与剩余问题。普通任务用 `Closes #编号`，共同任务的分段 PR 用 `Refs #编号`。
- 审核重点是能运行、输入输出一致、实验结论有依据，不把个人格式偏好或额外功能当作合并条件。
- 交付自己的代码时，顺手补齐相应 README 用法和报告中的方法、结果说明。不要把所有说明、结果整理和收尾工作留给组长。

完成标准：当前任务的验收内容已完成，相关检查已执行并如实记录，其他成员知道如何运行和在哪里查看结果。达到这个标准后就交付，不自行增加一轮工程化改造。

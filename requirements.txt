# 项目依赖。只记录实际验证过的版本，并注明用途；新增依赖请补一行说明。
# 已验证环境：Python 3.14.6（macOS arm64，CPU / MPS）。
torch==2.14.0        # 任务一、任务二模型训练与推理
numpy==2.5.3         # 窗口数组与坐标运算
pandas==3.0.6        # 窗口表、结构表与结果表读写
scikit-learn==1.9.1  # #4 起：多数类基线、聚类对照
matplotlib==3.11.2   # #4 起：混淆矩阵、Grad-CAM、轨道图

# 共享数据处理（#2）读取 .cool 需要 cooler，由该任务的负责人补上验证过的版本。
